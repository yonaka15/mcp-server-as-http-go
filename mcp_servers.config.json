{
  "version": "1.0",
  "servers": {
    "github": {
      "repository": "https://github.com/github/github-mcp-server.git",
      "build_command": "go mod tidy && go build -o github-mcp-server ./cmd/github-mcp-server",
      "command": "./github-mcp-server",
      "args": ["stdio"]
    }
  }
}
