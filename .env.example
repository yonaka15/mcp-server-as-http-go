# === MCP HTTP Server - Go Runtime Configuration ===

# === Core Server Configuration ===
MCP_CONFIG_FILE=mcp_servers.config.json
MCP_SERVER_NAME=code-sandbox
PORT=3000
HOST=0.0.0.0

# === Authentication ===
# Set your API key here to enable Bearer token authentication
HTTP_API_KEY=your-secret-api-key-here
# Set to 'true' to disable authentication completely  
DISABLE_AUTH=true

# === Go/Docker Runtime Configuration ===
WORK_DIR=/tmp/mcp-servers

# === Docker Configuration for code-sandbox-mcp ===
# Note: This will be overridden by docker-compose.yml for container communication
# DOCKER_HOST=unix:///var/run/docker.sock

# === Logging Configuration ===
# Set log level: error, warn, info, debug, trace
# For production: info, For development: debug
RUST_LOG=info

# === MCP Server Environment Variables ===

# Code Sandbox MCP Server (Go-based)
# Docker access is automatically configured via volume mount

# Redmine MCP Server
REDMINE_API_KEY=your-redmine-api-key
REDMINE_HOST=https://your-redmine-host.com

# GitHub MCP Server  
GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_your_token_here

# Brave Search MCP Server
BRAVE_API_KEY=your-brave-search-api-key

# === Docker Volume Configuration ===
# These paths are managed automatically by Docker
# MCP_CACHE_DIR=/tmp/mcp-servers
# NPM_CACHE_DIR=/app/.npm-cache

# === Development/Debug Options ===
# NODE_ENV=production
# DEBUG=mcp:*

# === Multi-Runtime Support ===
# Switch between runtimes by changing MCP_SERVER_NAME
# Supported values: code-sandbox, brave-search, github, redmine, filesystem

# === API Usage Examples ===
# Health Check:
#   curl -f http://localhost:3000/health
#
# List Tools:
#   curl -X POST http://localhost:3000/api/v1 \
#     -H "Content-Type: application/json" \
#     -d '{"command": "{\"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"tools/list\", \"params\": {}}"}'
#
# Initialize Sandbox:
#   curl -X POST http://localhost:3000/api/v1 \
#     -H "Content-Type: application/json" \
#     -d '{"command": "{\"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"tools/call\", \"params\": {\"name\": \"sandbox_initialize\", \"arguments\": {\"image\": \"python:3.12-slim\"}}}"}'