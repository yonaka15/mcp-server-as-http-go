# === MCP HTTP Server - Lightweight Configuration ===
# Uses node runtime for pre-built binary execution

# === Core Configuration ===
PORT=3000
DISABLE_AUTH=true
RUST_LOG=info

# === Optional Authentication ===
# HTTP_API_KEY=your-secret-api-key-here

# === API Usage Examples ===
# Health Check:
#   curl -f http://localhost:3000/health
#
# List Tools:
#   curl -X POST http://localhost:3000/api/v1 \
#     -H "Content-Type: application/json" \
#     -d '{"command": "{\"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"tools/list\", \"params\": {}}"}'
#
# Initialize Sandbox:
#   curl -X POST http://localhost:3000/api/v1 \
#     -H "Content-Type: application/json" \
#     -d '{"command": "{\"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"tools/call\", \"params\": {\"name\": \"sandbox_initialize\", \"arguments\": {\"image\": \"python:3.12-slim\"}}}"}'